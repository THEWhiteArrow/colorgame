<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Ranking</title>
   <link href="/css/style.css" rel="stylesheet" type="text/css">
</head>

<body>
   <h1 style="display: inline-flex; flex-direction: column;width:100%">
      <span>Global Ranking</span>
      <span style="margin-top:.5rem;font-size: .8em;">Mode : Hard</span>
   </h1>

   <ul id="ranking">
      <% let n=0 %>
      <% for(let score of scores) { %>
      <li>
         <div class="place">Place <span><%= l-n %></span> :</div>
         <div class="data"> <%=score.user  %> - <%= score.score %></div>
      </li>
      <% n+=1 %>
      <% } %>
   </ul>
   <div class="absolute">
      <a href="/">Back To Game</a>
   </div>
</body>

</html>